import { colorizeInterval } from 'math-facts/helpers/colorize-interval';
import { module, test } from 'qunit';

module('Unit | Helper | colorize interval');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = colorizeInterval([42]);
  assert.ok(result);
});
