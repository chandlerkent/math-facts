import { millisecondsToSeconds } from 'math-facts/helpers/milliseconds-to-seconds';
import { module, test } from 'qunit';

module('Unit | Helper | milliseconds to seconds');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = millisecondsToSeconds([42]);
  assert.ok(result);
});
